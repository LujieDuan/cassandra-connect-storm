matrix:
  include:
    - language: scala
      jdk: oraclejdk8
      before_script: cd data-source-simulator
      script:
        - sbt compile
      after_script: cd ..
    - language: scala
      jdk: oraclejdk8
      before_script: cd data-source-simulator
      script:
        - sbt compile
      after_script: cd ..
      
notifications:
  email: false